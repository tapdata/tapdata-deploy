apiVersion: v1
kind: Secret
metadata:
  name: mongodb-admin-password
  labels:
    {{- include "mongodb.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.global.mongodb.password | quote }}
